node_modules
dist
dist-ssr
# Per-run build output: `CYCASE_TEST_PORT=4283` builds into `dist-4283`, so two
# overlapping browser suites never share one output tree.
dist-*
coverage
*.local
.DS_Store
.vite
playwright-report
test-results
.assets-raw
.playwright-cli/

# Workflow worktrees are transient
.claude/worktrees/
*.tsbuildinfo

# Local run artefacts — screenshots and video a test run leaves behind
output/
.env
.env.*
!.env.example

# Generated release evidence. `scripts/release-gates.sh` writes a dated tree of
# gate logs here; it is a run record, not source, and it is regenerated by
# whoever runs the matrix.
docs/release/
